
import styles from './footer.module.scss'
import {Props} from './footer.constants'

export default function Footer ({ }: Props) {
  return (
    <section className={styles.wrapper}>

    </section>
  )
}